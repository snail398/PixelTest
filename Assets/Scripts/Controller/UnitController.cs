
public class UnitController 
{
    public Unit unit;

    public UnitController(Unit unit)
    {
        this.unit = unit;
    }

    public void UpdatePosition(float deltaTime)
    {
       // unit.position += new Vector(0.1f, 0.1f) * deltaTime;
    }
}
